struct inode;
struct inode_map;
struct directory_entry;
struct super_block;
struct path;
struct block_group;
// no need for the file descriptor abstraction. We'll let a file have its flags
struct file;
