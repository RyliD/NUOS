/**
This file contains declarations (and some definitions) for core components
of the filesystem. Not to be confused with the core functions (see ./fs/ops.h)
*/

// these type defs are just less to type.
typedef unsigned int uint;
typedef unsigned long ulong;

struct inode;
struct inode_map;
struct directory_entry;
struct super_block;
struct path;
struct block_group;
struct file;
